# Forensic audit remediation matrix A-001 to A-038

Baseline requested by the implementation plan: `main` at `1fb900c3d9c3f8322b5a97a0445b123386c3d70a`, with the pre-existing dirty worktree preserved. The completed worktree is integrated and revalidated against `origin/main` at `fe930654f878c6113facbc95f4ce541377f7a622`. This matrix records the final state of executable code, schema objects, migrations, and automated tests.

| ID | Final status | Executable and database evidence | Automated evidence |
|---|---|---|---|
| A-001 | FIXED-AND-TESTED | `config.ts` limits production entry points to DB/vault/role/port bootstrap; `operational-config.ts` is the typed DB provider with role scope, versions, restart state and AEAD; `import-operational-config.ts`, minimal systemd credentials and migrations `025`, `029` provide the upgrade path. | `config.test.ts`, `operational-config.test.ts`, `operational-config-db.test.ts`, `secrets.test.ts`, `test-split-service-config.sh`, clean DB bootstrap/import test. |
| A-002 | FIXED-AND-TESTED | `server-state.ts::setServerEnabled/transitionServerState` locks the server, rotates epochs, synchronizes managed-service state, revokes both token stores and audits in one transaction; admin API applies CSRF/recent-reauth; OAuth/discovery/call use the resulting state. | `server-state-db.test.ts` includes concurrent disable and token revocation; `admin-server-actions.test.ts`, `mcp.test.ts`, `auth-issue-token.test.ts`. |
| A-003 | FIXED-AND-TESTED | `admin-routes.ts::runServerTest` uses the active revision, production validators, lease, timeout, size limit, handler path and write-safe test policy; `main.tsx` renders status, correlation, revision, digest and redacted output. | `admin-server-actions.test.ts` covers pass, mismatch, timeout, invalid output and disabled state; `server-api.test.ts`. |
| A-004 | FIXED-AND-TESTED | `registration.ts` validates typed effect/annotation combinations and compiles both JSON schemas; `catalog.ts` exposes persisted active-revision hints; `mcp.ts` returns them in `tools/list`. | `registration.test.ts`, `catalog-contracts.test.ts`, `mcp.test.ts`. |
| A-005 | FIXED-AND-TESTED | `mcp-policy.ts::invokeWithDeadline` propagates `AbortSignal` according to shutdown policy; `mcp.ts` prevents late success/idempotency completion and releases leases in `finally`. | `mcp-policy.test.ts`, timeout cases in `mcp.test.ts` and `admin-server-actions.test.ts`. |
| A-006 | FIXED-AND-TESTED | Migration `026` adds per-server expiring leases; `acquireServerExecutionLease` uses atomic PostgreSQL locking, fails closed, and reports saturation; all callers release in `finally`. | `mcp-policy-db.test.ts` uses concurrent DB acquisitions; `mcp-policy.test.ts`, `mcp.test.ts`. |
| A-007 | FIXED-AND-TESTED | Typed effect and shutdown enums are persisted by migrations `009`/`026`; `mcp-policy.ts` maps them to explicit idempotency modes without text heuristics; digest conflicts and stale `PENDING` recovery are enforced in `mcp.ts`. | Parametric policy cases in `mcp-policy.test.ts`; idempotency contract cases in `mcp.test.ts`. |
| A-008 | FIXED-AND-TESTED | Migration `010` deterministically removes ineffective MCP `READ` and normalizes executable permission rows; runtime authorization accepts only `EXECUTE`. | `auth-permission.test.ts`, `catalog-contracts.test.ts`, `mcp.test.ts`. |
| A-009 | FIXED-AND-TESTED | Migration `010`, `auth.ts`, API schemas and `credential-pages.tsx` contain no effective `MANAGE` branch; managed-service scopes are explicit separately. | `auth-permission.test.ts`, `external-api-db.test.ts`, `credential-pages.test.tsx`. |
| A-010 | FIXED-AND-TESTED | `auth.ts::renameKajaCredential`, PATCH admin route and `RenameCredentialModal` enforce trim/length, authorization, `updated_at`, and redacted before/after audit. | Rename and authorization paths in `admin-server-actions.test.ts`; UI terminology/component tests. |
| A-011 | FIXED-AND-TESTED | `auth.ts` credential create/revoke/delete and permission replacement use `tx`, `FOR UPDATE`, prevalidation, epoch rotation, token revocation and audit in the same transaction. | `auth-permission.test.ts`, fault-injection cases in `external-api-db.test.ts`. |
| A-012 | FIXED-AND-TESTED | Managed and legacy permission/token stores are updated atomically; migrations `010`, `017`, `020`, `021` provide FK/unique constraints and no external side effect occurs inside permission transactions. | `external-api-db.test.ts` verifies rollback after token/audit failures and one logical token; `onboarding-db.test.ts`. |
| A-013 | FIXED-AND-TESTED | Migration `024` adds unique indexed `lookup_digest`; `sessionAccount` performs one indexed query and one Argon2 verify with no legacy scan; migration `031` adds account session epoch and rotations. | O(1) query assertion in `admin-routes-security.test.ts`; session/password/revoke tests in `admin-server-actions.test.ts`. |
| A-014 | FIXED-AND-TESTED | Migration `024` stores HMAC'd IP, account and IP+account buckets; admin login uses advisory transaction locks, progressive expiry, normalized accounts, trusted proxy policy, `Retry-After`, audit and alerts. | `admin-routes-security.test.ts`; real concurrent DB increments in `admin-security-db.test.ts`. |
| A-015 | FIXED-AND-TESTED | `secrets.ts` uses AES-256-GCM v2 with random nonce and account/purpose/key-id AAD; `rotateMfaEncryptionKey` re-encrypts locked accounts; recovery codes are Argon2 one-time hashes; migrations `023`/`033` and migration CLI remove plaintext. | `secrets.test.ts`, real key rotation in `operational-config-db.test.ts`, real legacy migration in `external-api-db.test.ts`, clean DB constraint validation. |
| A-016 | FIXED-AND-TESTED | Migration `027` introduces bootstrap state; `bootstrapAdmin` creates OWNER, MFA and recovery hashes atomically only when gated by local/trusted access or one-time secret, then irreversibly completes bootstrap. | Reuse/remote/bootstrap scenarios in `admin-role-routes.test.ts`; secret-field and bootstrap UI coverage in `auth-pages.test.tsx`. |
| A-017 | FIXED-AND-TESTED | Session identity comes from joined DB account data; runtime auth and deployment flows use the configurable bootstrap admin account rather than a hard-coded username; server-side `OWNER/ADMIN/AUDITOR` enforcement is centralized in the admin pre-handler. Legacy fixture/tooling literals remain outside the runtime auth path and are tracked separately. | `admin-role-routes.test.ts`, `app-layout.test.tsx`, `admin-server-actions.test.ts`. |
| A-018 | FIXED-AND-TESTED | Admin account/session/MFA/password/recovery APIs and `admin-pages.tsx` implement creation, role/active changes, password/MFA changes, session revocation and recovery rotation; last OWNER is protected transactionally. | `admin-role-routes.test.ts`, account management cases in `admin-server-actions.test.ts`. |
| A-019 | FIXED-AND-TESTED | `hostnames.ts` centralizes normalization/build/validation; `updateDomainConfiguration` atomically migrates DB hostnames, URLs and epochs; migration `032` removes domain-specific constraints; nginx is safely rendered from validated runtime domain values. | `hostnames.test.ts`, `operational-config.test.ts`, `test-render-nginx-config.sh`, release literal guard. |
| A-020 | FIXED-AND-TESTED | Migration `010` drops global `tool_name` uniqueness; routing compares the tool only within the hostname-selected server. | Same-name/different-host contract in `catalog-contracts.test.ts`; hostname isolation in `mcp.test.ts`. |
| A-021 | FIXED-AND-TESTED | `json-rpc.ts` owns parsing, exact 2.0 validation, ID rules, batch rejection, notification behavior and standard error codes; `mcp.ts` enforces media negotiation. | Parametric `json-rpc.test.ts`; transport cases in `mcp.test.ts`. |
| A-022 | FIXED-AND-TESTED | Central JSON-RPC/domain mapper returns safe application errors with `correlationId` after a valid envelope; transport errors remain HTTP before that boundary. | Error mapping and correlation assertions in `json-rpc.test.ts` and `mcp.test.ts`. |
| A-023 | FIXED-AND-TESTED | `BoundedValidatorCache` is revision/digest/server keyed with LRU bound and invalidation; registration AJV-compiles schemas before acceptance. | Hit/bound/invalidation cases in `mcp-policy.test.ts`; invalid schema registration cases in `registration.test.ts`. |
| A-024 | FIXED-AND-TESTED | Migration `026` and `mcp-policy.ts` implement atomic HMAC-keyed per-credential and per-server windows, with retry metadata and no plaintext key; HTTP store is PostgreSQL and fail-closed. | Credential isolation in `mcp.test.ts`; 100-way PostgreSQL serialization in `postgres-rate-limit-store-db.test.ts`. |
| A-025 | FIXED-AND-TESTED | `auth.ts` updates token `last_used_at` with write throttling after valid use without touching expiry; credential list separately aggregates issued and used timestamps. | Time/expiry assertions in `auth-issue-token.test.ts` and MCP usage tests. |
| A-026 | FIXED-AND-TESTED | API types expose `lastTokenIssuedAt` and `lastUsedAt`; `CredentialsPage` labels and renders them in separate columns. | `credential-pages.test.tsx`, `auth-permission.test.ts`. |
| A-027 | FIXED-AND-TESTED | Migration `028` versions typed monitoring profiles against registration revisions; `monitoring-policy.ts` validates profile/preview; worker reads DB targets; admin API/UI provide preview then versioned save. | `monitoring-policy.test.ts`, `monitoring.test.ts`, profile HTTP paths in `admin-server-actions.test.ts`, `server-api.test.ts`. |
| A-028 | FIXED-AND-TESTED | `monitoring-policy.ts::evaluateOperationalState` is deterministic with sample minimum and hysteresis; `setComputedOperationalState` only writes/audits actual transitions. | Transition table in `monitoring-policy.test.ts`; monitoring integration paths in `external-api-db.test.ts`. |
| A-029 | FIXED-AND-TESTED | `audit-view.ts` implements bounded cursor pagination, stable ordering, all requested filters, redaction and chain metadata; admin route streams bounded JSON export; `audit-page.tsx` provides filters/detail/export. | `audit-view.test.ts`, audit route tests in `admin-server-actions.test.ts`, `audit-db.test.ts`. |
| A-030 | FIXED-AND-TESTED | Logout is POST+CSRF and revokes only session context; revoke-all is separate, recent-reauth protected and rotates account session epoch; cookies retain secure host-only flags. | Logout scope/CSRF/session cases in `admin-server-actions.test.ts` and `admin-routes.test.ts`. |
| A-031 | FIXED-AND-TESTED | Migration `016` serializes canonical hash-chain append through a security-definer function; app DB role cannot mutate/truncate audit tables; migration `030` adds leased retrying archive outbox/checkpoint; monitor verifies and alerts. | `audit-db.test.ts`, `audit-archive.test.ts`, `test-db-role-isolation.sh`, readiness tests. |
| A-032 | FIXED-AND-TESTED | `mcp.ts` implements the supported Streamable HTTP POST subset explicitly, rejects fake GET/SSE/session behavior, validates Origin/media types and negotiates JSON. | Protocol harness cases in `mcp.test.ts` and release MCP smoke CLI. |
| A-033 | FIXED-AND-TESTED | MCP core performs generic `structuredContent`/optional content serialization; all domain adaptation lives in handlers and registry, with no Home Assistant/business-text branch in `mcp.ts`. | Generic output and multiple handler contract cases in `mcp.test.ts`, `reference-external-api.test.ts`, handler tests. |
| A-034 | FIXED-AND-TESTED | `common.tsx::Modal` implements initial focus, cyclic trap, Escape, ARIA naming/description, scroll lock and focus restoration. | `common.test.tsx` uses Testing Library, user-event and axe; `dialog-focus.test.ts` parametrizes cycle edges. |
| A-035 | FIXED-AND-TESTED | UI responsibilities are separated into `app-layout`, `auth-pages`, `server-api`, `credential-pages`, `admin-pages`, `operational-config-page`, `audit-page`, shared helpers/dialog and a state orchestrator. | `app-layout.test.tsx`, `server-api.test.ts`, component/helper suites, strict UI typecheck and Vite build. |
| A-036 | FIXED-AND-TESTED | UI consistently distinguishes “Klientské pověření Kaja” from a short-lived “Přístupový token”; internal legacy table names are not exposed as product terminology. | `credential-pages.test.tsx` protects both user-facing concepts. |
| A-037 | FIXED-AND-TESTED | Backend `sendError` returns a bounded safe code/message/correlation contract; `ui-helpers.ts` maps known codes to Czech, falls back safely and exposes only correlation detail. | Unsafe backend text in `errors.test.ts`, known/unknown/redaction cases in `ui-helpers.test.ts`, and JSON-RPC mapping tests. |
| A-038 | FIXED-AND-TESTED | `ui-helpers.ts` owns `Intl.DateTimeFormat("cs-CZ")` with DB-configured `uiTimeZone`; no executable UI path calls implicit `toLocaleString`. | Deterministic timezone/date cases in `ui-helpers.test.ts`; config runtime tests. |

## New database invariants

- `023`: access-token compatibility lookup and MFA recovery structure.
- `024`: unique indexed session lookup and three-scope login-throttle records.
- `025`/`029`: versioned operational settings, secret/non-secret shape constraint, applied-version tracking and `vault:v1` envelope constraint.
- `026`: typed MCP policy constraints, bounded concurrency leases, idempotency/rate state and supporting indexes.
- `027`: admin role/activation/bootstrap completion invariants and last-owner-capable data model.
- `028`: monitoring profile version/revision binding and profile digest.
- `030`: audit archive outbox state, lease and pending-work index.
- `031`: account session epoch for O(1) revoke-all invalidation.
- `032`: generic KCML code/hostname constraints independent of a production domain literal.
- `033`: enforced `NULL` or `enc:v2:*` MFA ciphertext format; release migration validates it after re-encryption.
- `034`: restores audit-writer privileges to the trusted SECURITY DEFINER owner in split-owner production databases while rejecting application-role ownership.
- `035`: grants that split-owner audit writer only the column-level read needed by `INSERT ... RETURNING id`.
- `036`: reasserts the audit writer's `SECURITY DEFINER`, fixed `search_path` and least-privilege owner contract after legacy upgrades.

## Verification record

- Frozen install, lint, strict typecheck and production builds passed; the non-DB suite finished with 53 files, 218 passing tests and 21 intentionally skipped PostgreSQL cases.
- Clean PostgreSQL migration applies all 37 migrations twice, including the split-owner audit-writer security contract, then completes the idempotent environment-to-vault import and MFA constraint validation (`clean-import-ok`).
- Production-schema fixture upgraded twice and returned `upgrade-ok` with all compatibility assertions.
- The final serial PostgreSQL run passed all eight files and all 21 tests, covering audit serialization, transactional onboarding/permissions, server state, leases, login/HTTP rate limits, operational config and external API runtime.
- All six deployment harnesses passed: release packaging, rendered nginx configuration, minimal systemd config split, DB role isolation, alert sink and restore permissions.
- A rendered Chromium audit passed at 1440x1000 and 390x844 with no axe violations, page exceptions or horizontal overflow; bootstrap secrets remained masked and invalid input produced a localized correlation-bearing error.
